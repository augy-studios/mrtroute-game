# 2.4 is the first release with allowed_contexts and allowed_installs on the
# command tree (bot.py). 3.x does not exist yet and may change the API.
discord.py>=2.4,<3

# The game API.
httpx>=0.27,<1
